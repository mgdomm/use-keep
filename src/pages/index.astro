---
import { getCollection } from 'astro:content';
import Base from '../layouts/Base.astro';

const allPosts = await getCollection('posts');
const featuredPosts = allPosts.filter(post => post.data.featured);
const recentPosts = allPosts.sort((a, b) => (b.data.pubDate?.getTime() || 0) - (a.data.pubDate?.getTime() || 0)).slice(0, 3);

const categories = [
  { name: 'Casa', slug: 'casa', desc: 'Cosas que liberan espacio y hacen más cómodo tu hogar' },
  { name: 'Trabajo', slug: 'trabajo', desc: 'Accesorios para trabajar mejor, más enfocado y sin dolor' },
  { name: 'Creatividad', slug: 'creatividad', desc: 'Herramientas simples que mejoran tu proceso creativo' },
  { name: 'Mascotas', slug: 'mascotas', desc: 'Objetos que facilitan la vida con tus mascotas' },
  { name: 'Familia', slug: 'familia', desc: 'Pequeñas cosas que hacen la vida familiar más fácil' }
];

const categoryPaths = {
  'Casa': '/casa/',
  'Trabajo': '/trabajo/',
  'Creatividad': '/creatividad/',
  'Mascotas': '/mascotas/',
  'Familia': '/familia/'
};
---

<Base title="Pequeñas cosas que se quedan contigo" description="Objetos simples, prácticos y cotidianos que no sabías que necesitabas, hasta que los pruebas.">
  <div class="homepage">
    <section class="hero">
      <h1>Pequeñas cosas que hacen la vida más fácil</h1>
      <p>Objetos simples, prácticos y cotidianos que no sabías que necesitabas… hasta que los pruebas.</p>
      <div class="hero-ctas">
        <a href="#imprescindibles" class="btn btn-primary">Ver imprescindibles</a>
        <a href="#articulos" class="btn btn-secondary">Descubrir ideas prácticas</a>
      </div>
    </section>

    <section class="features">
      <h2>✨ Lo que encontrarás aquí</h2>
      <div class="features-grid">
        <div class="feature">
          <span class="feature-icon">✓</span>
          <h3>Productos pequeños pero útiles</h3>
        </div>
        <div class="feature">
          <span class="feature-icon">✓</span>
          <h3>Nada de compras absurdas</h3>
        </div>
        <div class="feature">
          <span class="feature-icon">✓</span>
          <h3>Pensado para el día a día</h3>
        </div>
        <div class="feature">
          <span class="feature-icon">✓</span>
          <h3>Recomendaciones claras y honestas</h3>
        </div>
      </div>
    </section>

    <section class="categories">
      <h2>🏠 Categorías principales</h2>
      <div class="categories-grid">
        {categories.map(cat => (
          <a href={categoryPaths[cat.name as keyof typeof categoryPaths]} class="category-card">
            <h3>{cat.name}</h3>
            <p>{cat.desc}</p>
          </a>
        ))}
      </div>
    </section>

    <section id="imprescindibles" class="featured">
      <h2>Imprescindibles de la vida diaria</h2>
      <div class="featured-products">
        {featuredPosts.map(post => (
          <article class="featured-card">
            <h3>{post.data.title}</h3>
            <p>{post.data.description}</p>
            <a href={`/${post.slug}/`} class="btn btn-small">Descubrir artículo →</a>
          </article>
        ))}
      </div>
    </section>

    <section id="articulos" class="recent">
      <h2>Artículos recientes</h2>
      <div class="articles-list">
        {recentPosts.map(post => (
          <article class="article-item">
            <h3>{post.data.title}</h3>
            <p class="article-meta">{post.data.category}</p>
            <p class="article-description">{post.data.description}</p>
            <a href={`/${post.slug}/`} class="read-more">Leer más →</a>
          </article>
        ))}
      </div>
    </section>

    <section class="newsletter">
      <h2>Nuevos descubrimientos cada semana</h2>
      <p>Recibe artículos directos en tu correo. Nada de spam, solo objetos prácticos.</p>
      <form class="newsletter-form">
        <input type="email" placeholder="tu@correo.com" required>
        <button type="submit" class="btn btn-primary">Suscribirse</button>
      </form>
    </section>
  </div>
</Base>

<style>
  .homepage {
    max-width: 1200px;
    margin: 0 auto;
  }

  .hero {
    text-align: center;
    padding: 5rem 2rem 4rem;
    margin-bottom: 5rem;
    position: relative;
  }

  .hero::after {
    content: '';
    position: absolute;
    bottom: -2rem;
    left: 50%;
    transform: translateX(-50%);
    width: 60px;
    height: 2px;
    background: linear-gradient(to right, transparent, #7FA895, transparent);
  }

  .hero h1 {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1.2rem;
    line-height: 1.2;
    color: #2D3436;
    letter-spacing: -0.5px;
  }

  .hero p {
    font-size: 1.25rem;
    color: #7FA895;
    margin-bottom: 2.5rem;
    max-width: 650px;
    margin-left: auto;
    margin-right: auto;
    font-weight: 500;
    line-height: 1.6;
  }

  .hero-ctas {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-block;
    padding: 0.95rem 1.8rem;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 700;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
    font-size: 0.95rem;
  }

  .btn-primary {
    background: linear-gradient(135deg, #7FA895 0%, #6B9483 100%);
    color: white;
    box-shadow: 0 4px 12px rgba(127, 168, 149, 0.25);
  }

  .btn-primary:hover {
    background: linear-gradient(135deg, #6B9483 0%, #5A8174 100%);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(127, 168, 149, 0.35);
  }

  .btn-secondary {
    background: white;
    color: #7FA895;
    border: 2px solid #7FA895;
    box-shadow: 0 2px 8px rgba(127, 168, 149, 0.1);
  }

  .btn-secondary:hover {
    background: #F8F7F5;
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(127, 168, 149, 0.2);
  }

  .btn-small {
    padding: 0.65rem 1.2rem;
    font-size: 0.85rem;
  }

  .features {
    margin-bottom: 4rem;
  }

  .features h2,
  .featured h2,
  .recent h2,
  .newsletter h2 {
    font-size: 2rem;
    margin-bottom: 2rem;
    text-align: center;
    color: #2D3436;
    font-weight: 700;
    letter-spacing: -0.3px;
  }

  .features {
    margin-bottom: 5rem;
  }

  .features h2 {
    position: relative;
    padding-bottom: 1.5rem;
  }

  .features h2::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 40px;
    height: 2px;
    background: #7FA895;
    border-radius: 1px;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
  }

  .feature {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(232, 240, 235, 0.4) 100%);
    padding: 2rem;
    border-radius: 8px;
    border: 1px solid #E8F0EB;
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    box-shadow: 0 4px 15px rgba(127, 168, 149, 0.08);
    transition: all 0.3s ease;
  }

  .feature:hover {
    border-color: #7FA895;
    box-shadow: 0 12px 32px rgba(127, 168, 149, 0.18);
    transform: translateY(-4px);
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.98) 0%, rgba(232, 240, 235, 0.6) 100%);
  }

  .feature-icon {
    font-size: 2rem;
    flex-shrink: 0;
    margin-top: 0.25rem;
  }

  .feature h3 {
    font-size: 1.05rem;
    color: #2D3436;
    font-weight: 700;
    margin: 0;
  }

  .categories {
    margin-bottom: 5rem;
    position: relative;
  }

  .categories h2 {
    position: relative;
    padding-bottom: 1.5rem;
  }

  .categories h2::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 40px;
    height: 2px;
    background: #7FA895;
    border-radius: 1px;
  }

  .categories-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
  }

  .category-card {
    background: linear-gradient(135deg, #FFFFFF 0%, rgba(232, 240, 235, 0.3) 100%);
    padding: 2rem;
    border-radius: 8px;
    border: 1px solid #E8F0EB;
    text-decoration: none;
    color: #2D3436;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(127, 168, 149, 0.08);
    position: relative;
    overflow: hidden;
  }

  .category-card::before {
    content: '';
    position: absolute;
    top: 0;
    right: -20px;
    width: 60px;
    height: 60px;
    background: rgba(127, 168, 149, 0.05);
    border-radius: 50%;
    transition: all 0.3s ease;
  }

  .category-card:hover {
    border-color: #7FA895;
    transform: translateY(-6px);
    box-shadow: 0 16px 40px rgba(127, 168, 149, 0.18);
    background: linear-gradient(135deg, #FFFFFF 0%, rgba(232, 240, 235, 0.5) 100%);
  }

  .category-card:hover::before {
    right: -10px;
    width: 80px;
    height: 80px;
  }

  .category-card h3 {
    font-size: 1.4rem;
    margin-bottom: 0.75rem;
    color: #2D3436;
    font-weight: 700;
    position: relative;
    z-index: 1;
  }

  .category-card p {
    color: #7FA895;
    font-size: 0.95rem;
    position: relative;
    z-index: 1;
    font-weight: 500;
    line-height: 1.5;
  }

  .featured {
    margin-bottom: 5rem;
  }

  .featured h2 {
    position: relative;
    padding-bottom: 1.5rem;
    margin-bottom: 3rem;
  }

  .featured h2::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 40px;
    height: 2px;
    background: #7FA895;
    border-radius: 1px;
  }

  .featured-products {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2.5rem;
  }

  .featured-card {
    background: linear-gradient(135deg, #FFFFFF 0%, rgba(232, 240, 235, 0.35) 100%);
    padding: 2.5rem;
    border-radius: 10px;
    border: 1px solid #E8F0EB;
    box-shadow: 0 6px 20px rgba(127, 168, 149, 0.1);
    transition: all 0.4s ease;
    position: relative;
  }

  .featured-card::before {
    content: '✨';
    position: absolute;
    top: -12px;
    left: 2rem;
    font-size: 2rem;
    opacity: 0;
    transition: all 0.4s ease;
  }

  .featured-card:hover {
    border-color: #7FA895;
    box-shadow: 0 16px 48px rgba(127, 168, 149, 0.2);
    transform: translateY(-8px);
    background: linear-gradient(135deg, #FFFFFF 0%, rgba(232, 240, 235, 0.5) 100%);
  }

  .featured-card:hover::before {
    opacity: 1;
    top: 0;
  }

  .featured-card h3 {
    font-size: 1.3rem;
    margin-bottom: 1rem;
    color: #2D3436;
    font-weight: 700;
  }

  .featured-card p {
    color: #7FA895;
    margin-bottom: 1.5rem;
    font-weight: 500;
    line-height: 1.6;
  }

  .recent {
    margin-bottom: 5rem;
  }

  .recent h2 {
    position: relative;
    padding-bottom: 1.5rem;
    margin-bottom: 3rem;
  }

  .recent h2::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 40px;
    height: 2px;
    background: #7FA895;
    border-radius: 1px;
  }

  .articles-list {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .article-item {
    background: linear-gradient(135deg, #FFFFFF 0%, rgba(232, 240, 235, 0.25) 100%);
    padding: 2rem;
    border-radius: 8px;
    border: 1px solid #E8F0EB;
    box-shadow: 0 4px 15px rgba(127, 168, 149, 0.08);
    transition: all 0.3s ease;
    border-left: 4px solid transparent;
  }

  .article-item:hover {
    border-color: #7FA895;
    border-left-color: #7FA895;
    box-shadow: 0 12px 32px rgba(127, 168, 149, 0.15);
    transform: translateX(4px);
  }

  .article-item h3 {
    font-size: 1.35rem;
    margin-bottom: 0.5rem;
    color: #2D3436;
    font-weight: 700;
  }

  .article-meta {
    color: #B8B5B0;
    font-size: 0.85rem;
    margin-bottom: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .article-description {
    color: #7FA895;
    margin-bottom: 1.2rem;
    font-weight: 500;
    line-height: 1.6;
  }

  .read-more {
    color: #7FA895;
    text-decoration: none;
    font-weight: 700;
    transition: all 0.3s ease;
    display: inline-block;
  }

  .read-more:hover {
    color: #2D3436;
    transform: translateX(4px);
  }

  .newsletter {
    background: linear-gradient(135deg, #FFFFFF 0%, rgba(232, 240, 235, 0.4) 100%);
    padding: 4rem 2rem;
    border-radius: 10px;
    border: 1px solid #E8F0EB;
    text-align: center;
    margin-bottom: 2rem;
    box-shadow: 0 6px 20px rgba(127, 168, 149, 0.1);
    position: relative;
    overflow: hidden;
  }

  .newsletter::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -50%;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(127, 168, 149, 0.05) 0%, transparent 70%);
    border-radius: 50%;
    pointer-events: none;
  }

  .newsletter h2 {
    margin-bottom: 0.75rem;
    color: #2D3436;
    position: relative;
    z-index: 1;
    font-weight: 700;
  }

  .newsletter p {
    color: #7FA895;
    margin-bottom: 2rem;
    position: relative;
    z-index: 1;
    font-weight: 500;
    font-size: 1.05rem;
  }

  .newsletter-form {
    display: flex;
    gap: 0.75rem;
    max-width: 550px;
    margin: 0 auto;
    flex-wrap: wrap;
    justify-content: center;
    position: relative;
    z-index: 1;
  }

  .newsletter-form input {
    flex: 1;
    min-width: 200px;
    padding: 1rem;
    border: 2px solid #E8F0EB;
    border-radius: 6px;
    font-size: 0.95rem;
    background: white;
    color: #2D3436;
    transition: all 0.3s ease;
    font-weight: 500;
  }

  .newsletter-form input::placeholder {
    color: #B8B5B0;
  }

  .newsletter-form input:focus {
    outline: none;
    border-color: #7FA895;
    background: #FAFAF8;
    box-shadow: 0 0 0 4px rgba(127, 168, 149, 0.15);
  }

  .newsletter .btn-primary {
    font-weight: 700;
    box-shadow: 0 4px 12px rgba(127, 168, 149, 0.2);
  }

  .newsletter .btn-primary:hover {
    box-shadow: 0 8px 24px rgba(127, 168, 149, 0.3);
  }

  @media (max-width: 768px) {
    .hero h1 {
      font-size: 1.8rem;
    }

    .hero-ctas {
      flex-direction: column;
    }

    .btn {
      width: 100%;
      text-align: center;
    }
  }
</style>
